const endpoints = {
  login: `${process.env.NEXT_PUBLIC_ENPOINT_URL}/auth/login`,
  user: `${process.env.NEXT_PUBLIC_ENPOINT_URL}/user`,
  product: `${process.env.NEXT_PUBLIC_ENPOINT_URL}/shop`,
};

export default endpoints;
